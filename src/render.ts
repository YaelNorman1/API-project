class Render{


    renderPage(object: Object) {         //user: User, quote: Quote, pokemon: Pokemon, aboutMe: AboutMe
        console.log("hello from render");
        console.log(object);        //user, quote, pokemon, aboutMe
    }
}